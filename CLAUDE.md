# CLAUDE.md — 開発運用ルール

本リポジトリ（`water_temperature`）で Claude / 開発者が守る運用ルールを定める。
**このファイルの内容は徹底すること。**

## 1. コミュニケーション

- **対話は日本語**で行う。
- コミットメッセージ・PR・コード内コメントも原則日本語（技術用語の英語は可）。

## 2. 言語・実装方針

- 実装は **Rust を第一選択**（embassy / `cyw43` / `trouble-host`）。
- Rust での実現が困難な場合に限り **C（Pico SDK + BTstack）** へ切り替える。
- **Python は禁止**（ファームウェア・テスト・スクリプトいずれも不可）。

## 3. ブランチ戦略

| ブランチ | 役割 | 直接 push |
| --- | --- | --- |
| `main` | 本番（リリース） | 不可（PR 経由） |
| `develop` | 開発統合 | 不可（PR 経由） |
| 作業ブランチ（`claude/*`, `feature/*`） | 個別機能の実装 | 可 |

- 実装は必ず**作業ブランチ**で行い、**`develop` 向けに PR** を発行する。
- `develop` がまとまったら `develop` → `main` の PR で本番反映する。
- PR は原則 **Draft** で作成し、レビュー準備が整ったら Ready にする。

## 4. Copilot レビュー対応フロー（重要）

PR のレビューは **GitHub Copilot** が実施する。指摘への対応は以下を厳守する。

1. **勝手に対応しない。** Copilot の指摘を受けたら、まず内容を精査する。
2. **指摘事項と対応方針を一覧表にまとめ、ユーザーに可否を確認する。**
   - 形式例:

     | # | ファイル:行 | 指摘内容（要約） | 対応方針 | 対応可否 |
     | --- | --- | --- | --- | --- |
     | 1 | `src/ble.rs:120` | ○○ | △△に修正 | 要確認 |

3. **ユーザーの可否応答を得てから**、可となった項目のみ対応（コード修正）する。
   - 「対応しない」と判断した指摘は、その理由を Copilot のコメントに返信する。
4. 対応・返信が済んだレビューコメントは**該当スレッドを resolve** する。
5. すべて処理したら**次のレビューを待機**する（勝手に次の変更へ進まない）。

> ポイント: 「精査 → 一覧化 → 可否確認 → 対応/返信 → resolve → 待機」を 1 サイクルとする。

## 5. ドキュメント維持

コード変更時、関連する以下のドキュメントを更新すること。

| ファイル | 内容 |
| --- | --- |
| `specification.md` | 詳細仕様（ハード構成・BLE 定義・温度変換等） |
| `environment.md` | ローカル環境構築・実機セットアップ手順 |
| `test_procedure.md` | テスト手順（ホスト/CI/実機） |
| `README.md` | 概要 |

## 6. テスト・CI

- ハードウェア非依存ロジックは `core` クレートに置き、**ホストで `cargo test`** 可能にする。
- CI（`.github/workflows/ci.yml`）で fmt / clippy / `core` テスト / firmware クロスビルドを検証する。
- 実機を要する検証は `test_procedure.md` に手順化し、手動または将来のセルフホスト Runner で実施する。

## 7. ローカル作業ディレクトリの制約

- ローカルのクローン先は **`D:\work\dev`** 配下のみ利用する。
- **上記ディレクトリ以外のローカルファイルには絶対に触れない。**
